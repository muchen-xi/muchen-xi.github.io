# Wrangler configuration for CF Pages project "248200-xyz"
# KV binding for PV counting pipeline
#
# Once authenticated (wrangler login), deploy with:
#   npx wrangler pages deploy . --project-name=248200-xyz --branch=main

pages_build_output_dir = "."

[[kv_namespaces]]
binding = "SITE_ANALYTICS"
id = "132f0237e83845caa4325effad690cee"
